import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    title: {
        fontSize: 14,
        textAlign:'center'
    },
    counter: {
        fontWeight: 'bold',
        fontSize: 18,
        textAlign:'center'
    }
});

export default styles;
