import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex:1, padding:1
    },
    galleryImage:{
        flex:1,
        height: 130,
        width: "100%",
    }
});

export default styles;
